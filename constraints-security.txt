# Security floor pins (High/Critical CVE remediation — 2026-07-25 pip-audit)
# Use: pip install -r requirements.txt -c constraints-security.txt
#
# Auth / crypto
cryptography>=48.0.1
PyJWT>=2.13.0
joserfc>=1.6.8

# HTTP / ASGI / uploads
starlette>=1.3.1
python-multipart>=0.0.31
aiohttp>=3.14.1
idna>=3.15

# Document ingest (untrusted files)
pillow>=12.3.0
pypdf>=6.14.2

# Knowledge graph / MCP
lightrag-hku>=1.5.4
mcp>=1.28.1

# Agent stack
langgraph-checkpoint>=4.1.1
langgraph-sdk>=0.3.15
langsmith>=0.8.18

# Config
pydantic-settings>=2.14.2

# Embeddings stack (transitive)
onnx>=1.22.0
# torch: install CPU wheels only (see Dockerfile TORCH_CPU_INDEX / requirements.txt).
# Do not pin a CUDA torch build here — that reintroduces nvidia-* packages.

# Parsing helpers
soupsieve>=2.8.4
json-repair>=0.60.1
pyasn1>=0.6.4

# Tooling (image hygiene)
pip>=26.1.2
setuptools>=83.0.0
