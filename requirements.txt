# Core
fastapi[standard]>=0.136.0
# Security floors (see constraints-security.txt / docs)
starlette>=1.3.1
python-multipart>=0.0.31
fastmcp>=3.0.0
mcpo>=0.0.15
langgraph>=0.3.0
langgraph-checkpoint>=4.1.1
langgraph-sdk>=0.3.15
langsmith>=0.8.18
pydantic>=2.9.0
pydantic-settings>=2.14.2

# Auth
PyJWT>=2.13.0
cryptography>=48.0.1
joserfc>=1.6.8

# Ingestion
unstructured[pdf,docx,xlsx]>=0.22.0
pdfplumber>=0.11.0  # digital PDF table + prose extraction
pypdf>=6.14.2
pillow>=12.3.0
pytesseract>=0.3.10  # OCR for embedded images / scanned crops (needs tesseract binary)
python-docx>=1.2.0
python-pptx>=1.0.2
openpyxl>=3.1.0
xlrd>=2.0.1  # legacy binary .xls parsing (openpyxl only reads .xlsx)

# Embeddings & Vector DB
sentence-transformers>=3.3.0
einops>=0.8.0
lancedb>=0.30.0
onnx>=1.22.0
# torch is a transitive dep of sentence-transformers (local embeddings + CrossEncoder).
# Do NOT install default PyPI torch on Linux — it pulls multi-GB nvidia-* CUDA wheels.
# The Dockerfile installs CPU-only torch/torchvision from
# https://download.pytorch.org/whl/cpu (override with TORCH_CPU_INDEX build-arg).
# For local dev on Linux, prefer the same:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt -c constraints-security.txt

# Knowledge Graph
lightrag-hku>=1.5.4

# MCP
mcp>=1.28.1

# Web Crawling
beautifulsoup4>=4.12.0
markdownify>=0.13.0
soupsieve>=2.8.4
aiohttp>=3.14.1
idna>=3.15

# LLM
openai>=1.55.0

# Database (metadata store)
sqlalchemy>=2.0.36
aiosqlite>=0.20.0

# Tabular store (structured spreadsheet querying)
duckdb>=1.0.0

# Misc transitive security floors
json-repair>=0.60.1
pyasn1>=0.6.4

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
